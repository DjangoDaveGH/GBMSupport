{
  "name": "hyport-functions",
  "description": "Cloud Functions for Hyperion Support — admin user provisioning and ticket-lifecycle notifications",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.1.1"
  }
}
