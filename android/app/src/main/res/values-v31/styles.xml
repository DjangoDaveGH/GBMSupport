<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31+) replaced the old windowBackground-drawable splash
     mechanism with its own native SplashScreen API — without this override,
     values/styles.xml's LaunchTheme is ignored on API 31+ devices (i.e. most
     real phones) and the OS shows its own default splash (just the launcher
     icon on a plain background) instead of the branded navy+crest screen. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_inset</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
