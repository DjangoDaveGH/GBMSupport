<?xml version="1.0" encoding="utf-8"?>
<!-- Branded launch screen: navy background + the real Ghana Coat of Arms,
     matching SplashScreen (lib/features/auth/presentation/splash_screen.dart)
     so there's no unbranded white flash before the Flutter engine paints.
     This v21 variant is what actually applies on real devices (API 21+) —
     drawable/launch_background.xml alone would be overridden by this one. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
